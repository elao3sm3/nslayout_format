# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'nslayout_format' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for nslayout_format

  target 'nslayout_formatTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'nslayout_formatUITests' do
    inherit! :search_paths
    # Pods for testing
  end
pod "SnapKit"
end
